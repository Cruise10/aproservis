<section class="destacar">

  <div class="container-destacar">

    <div class="destacar-header">
      <span class="subtitulo">
        <svg width="12" height="12" viewBox="0 0 24 24" fill="var(--color-naranja)"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
        POR QUÉ ELEGIRNOS
      </span>
      <h2>Qué nos hace <em>destacar</em></h2>
      <p>Nos diferenciamos por nuestra visión de liderazgo, misión de calidad y un compromiso
      real con la seguridad en cada proyecto que ejecutamos.</p>
    </div>

    <div class="grid-destacar">

      <div class="card-destacar">
        <div class="icono-circulo">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><polyline points="12 16 12.01 16"/><path d="M12 2a10 10 0 0 1 7.07 17.07"/></svg>
        </div>
        <div class="card-numero">01</div>
        <h3>Nuestra Visión</h3>
        <p>Ser empresa líder a nivel nacional brindando servicios de contratación, construcción civil e ingeniería, con estándares de calidad internacional y compromiso con la excelencia.</p>
        <div class="card-linea"></div>
      </div>

      <div class="card-destacar card-central">
        <div class="icono-circulo">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>
        </div>
        <div class="card-numero">02</div>
        <h3>Nuestra Misión</h3>
        <p>Ser una empresa competitiva y comprometida en brindar servicios de alta calidad, conformada por un equipo calificado con énfasis en la mejora continua y la satisfacción del cliente.</p>
        <div class="card-linea"></div>
      </div>

      <div class="card-destacar">
        <div class="icono-circulo">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
        </div>
        <div class="card-numero">03</div>
        <h3>Política de Seguridad</h3>
        <p>Evaluamos cada actividad con todos los niveles laborales, eliminando riesgos, capacitando al personal y proveyendo condiciones necesarias para la ejecución segura de cada obra.</p>
        <div class="card-linea"></div>
      </div>

    </div>

    <div class="destacar-stats">
      <div class="stat-item">
        <strong>+200</strong>
        <span>Proyectos</span>
      </div>
      <div class="stat-sep"></div>
      <div class="stat-item">
        <strong>10+</strong>
        <span>Años</span>
      </div>
      <div class="stat-sep"></div>
      <div class="stat-item">
        <strong>IBNORCA</strong>
        <span>Certificados</span>
      </div>
      <div class="stat-sep"></div>
      <div class="stat-item">
        <strong>20.000</strong>
        <span>PSI en pruebas</span>
      </div>
    </div>

  </div>

</section>

<style>

  .destacar {
    padding: 100px 20px;
    background: #f5f5f5;
    position: relative;
    overflow: hidden;
  }

  /* Decoración de fondo */
  .destacar::before {
    content: '';
    position: absolute;
    top: -80px;
    right: -80px;
    width: 320px;
    height: 320px;
    background: radial-gradient(circle, rgba(246,127,35,0.08) 0%, transparent 70%);
    border-radius: 50%;
    pointer-events: none;
  }

  .container-destacar {
    max-width: 1200px;
    margin: auto;
    position: relative;
  }

  /* ENCABEZADO */
  .destacar-header {
    text-align: center;
    margin-bottom: 60px;
  }

  .subtitulo {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    color: var(--color-naranja);
    font-weight: 700;
    letter-spacing: 2px;
    font-size: 12px;
    text-transform: uppercase;
    margin-bottom: 14px;
  }

  .destacar-header h2 {
    font-size: clamp(28px, 4vw, 42px);
    font-weight: 800;
    color: #1a1a1a;
    letter-spacing: -0.5px;
    margin: 0 0 16px;
  }

  .destacar-header h2 em {
    font-style: normal;
    color: var(--color-naranja);
  }

  .destacar-header p {
    max-width: 520px;
    margin: 0 auto;
    color: #777;
    font-size: 16px;
    line-height: 1.7;
  }

  /* GRID */
  .grid-destacar {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
    margin-bottom: 48px;
  }

  /* CARD */
  .card-destacar {
    background: white;
    padding: 36px 30px;
    border-radius: 16px;
    box-shadow: 0 4px 20px rgba(0,0,0,0.06);
    position: relative;
    overflow: hidden;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .card-destacar:hover {
    transform: translateY(-8px);
    box-shadow: 0 20px 48px rgba(0,0,0,0.12);
  }

  /* Card central destacada */
  .card-central {
    background: #1a1a1a;
    color: white;
  }

  .card-central h3 { color: white !important; }
  .card-central p  { color: rgba(255,255,255,0.75) !important; }
  .card-central .card-numero { color: rgba(255,255,255,0.15); }
  .card-central .card-linea { background: var(--color-naranja); }

  /* NÚMERO DECORATIVO */
  .card-numero {
    position: absolute;
    top: 20px;
    right: 24px;
    font-size: 52px;
    font-weight: 800;
    color: rgba(0,0,0,0.05);
    line-height: 1;
    pointer-events: none;
    user-select: none;
  }

  /* ICONO */
  .icono-circulo {
    width: 60px;
    height: 60px;
    background: rgba(246, 127, 35, 0.12);
    border-radius: 14px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-naranja);
    margin-bottom: 4px;
  }

  .card-central .icono-circulo {
    background: rgba(246, 127, 35, 0.2);
  }

  /* TEXTO */
  .card-destacar h3 {
    font-size: 18px;
    font-weight: 700;
    color: #1a1a1a;
    margin: 0;
  }

  .card-destacar p {
    font-size: 14px;
    color: #777;
    line-height: 1.7;
    margin: 0;
  }

  /* LÍNEA INFERIOR */
  .card-linea {
    height: 3px;
    width: 40px;
    background: var(--color-naranja);
    border-radius: 2px;
    margin-top: 8px;
    transition: width 0.3s ease;
  }

  .card-destacar:hover .card-linea {
    width: 80px;
  }

  /* STATS */
  .destacar-stats {
    background: white;
    border-radius: 14px;
    padding: 32px 40px;
    display: flex;
    align-items: center;
    justify-content: space-around;
    box-shadow: 0 4px 20px rgba(0,0,0,0.06);
    flex-wrap: wrap;
    gap: 24px;
  }

  .stat-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 4px;
  }

  .stat-item strong {
    font-size: 32px;
    font-weight: 800;
    color: var(--color-naranja);
    line-height: 1;
  }

  .stat-item span {
    font-size: 13px;
    color: #888;
    font-weight: 500;
  }

  .stat-sep {
    width: 1px;
    height: 40px;
    background: #eee;
  }

  /* RESPONSIVE */
  @media (max-width: 900px) {
    .grid-destacar {
      grid-template-columns: 1fr;
      gap: 16px;
    }

    .card-central {
      order: -1; /* la card oscura primero en móvil */
    }

    .stat-sep { display: none; }

    .destacar-stats {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      padding: 28px 24px;
      gap: 20px;
    }

    .stat-item { align-items: flex-start; }
  }

  @media (max-width: 560px) {
    .destacar {
      padding: 64px 16px;
    }

    .destacar-header {
      margin-bottom: 40px;
    }

    .card-destacar {
      padding: 28px 22px;
    }
  }
</style>