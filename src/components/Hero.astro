<section class="hero">

  <div class="hero-content">

    <span class="hero-badge">
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2L15.09 8.26L22 9.27L17 14.14L18.18 21.02L12 17.77L5.82 21.02L7 14.14L2 9.27L8.91 8.26L12 2Z"/></svg>
      Añez Proyectos &amp; Servicios — Bolivia
    </span>

    <h1 class="hero-title">
      Construcciones Civiles,<br>
      <em>Industriales</em> y Mecánicas
    </h1>

    <p class="hero-desc">
      Empresa proveedora de servicios con altos niveles de seguridad, excelencia
      y mejoramiento continuo. Tu aliado estratégico en cada proyecto.
    </p>

    <div class="hero-buttons">
      <a href="/contacto" class="btn-hero-primary">
        Solicitar Cotización
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
      </a>
      <a href="/proyectos" class="btn-hero-secondary">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="5 3 19 12 5 21 5 3"/></svg>
        Ver Proyectos
      </a>
    </div>

    <!-- Stats -->
    <div class="hero-stats">
      <div class="stat">
        <strong>+200</strong>
        <span>Proyectos ejecutados</span>
      </div>
      <div class="stat-divider"></div>
      <div class="stat">
        <strong>5+</strong>
        <span>Años de experiencia</span>
      </div>
      <div class="stat-divider"></div>
      <div class="stat">
        <strong>IBNORCA</strong>
        <span>Certificados</span>
      </div>
    </div>

  </div>

  <!-- Scroll indicator -->
  <div class="scroll-indicator">
    <div class="scroll-line"></div>
  </div>

</section>

<style>

  .hero {
    min-height: 100vh;
    background:
      linear-gradient(to bottom, rgba(0,0,0,0.55) 0%, rgba(0,0,0,0.35) 60%, rgba(0,0,0,0.65) 100%),
      url("/images/banner.jpg");
    background-size: cover;
    background-position: center;

    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    color: white;
    position: relative;
    padding: 120px 20px 80px;
    overflow: hidden;
  }

  /* Grain overlay sutil */
  .hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.04'/%3E%3C/svg%3E");
    pointer-events: none;
    opacity: 0.4;
  }

  .hero-content {
    position: relative;
    z-index: 20;
    max-width: 780px;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0;
  }

  /* BADGE */
  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    background: rgba(246, 127, 35, 0.2);
    border: 1px solid rgba(246, 127, 35, 0.5);
    color: #ffb472;
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 0.5px;
    padding: 7px 16px;
    border-radius: 50px;
    margin-bottom: 28px;
    animation: fadeDown 0.7s ease both;
  }

  /* TÍTULO */
  .hero-title {
    font-size: clamp(36px, 6vw, 68px);
    font-weight: 800;
    line-height: 1.1;
    margin: 0 0 24px;
    letter-spacing: -1px;
    animation: fadeDown 0.7s ease 0.15s both;
  }

  .hero-title em {
    font-style: normal;
    color: var(--color-naranja);
    position: relative;
  }

  /* Subrayado decorativo en la palabra em */
  .hero-title em::after {
    content: '';
    position: absolute;
    bottom: 2px;
    left: 0;
    right: 0;
    height: 3px;
    background: var(--color-naranja);
    border-radius: 2px;
    opacity: 0.5;
  }

  /* DESCRIPCIÓN */
  .hero-desc {
    font-size: clamp(16px, 2vw, 19px);
    color: rgba(255,255,255,0.82);
    line-height: 1.7;
    margin: 0 0 40px;
    max-width: 560px;
    animation: fadeDown 0.7s ease 0.28s both;
  }

  .br-desktop { display: block; }

  /* BOTONES */
  .hero-buttons {
    display: flex;
    gap: 16px;
    align-items: center;
    flex-wrap: wrap;
    justify-content: center;
    margin-bottom: 60px;
    animation: fadeDown 0.7s ease 0.4s both;
  }

  .btn-hero-primary {
    background: var(--color-naranja);
    color: white;
    padding: 14px 28px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 700;
    font-size: 15px;
    display: flex;
    align-items: center;
    gap: 9px;
    transition: background 0.25s, transform 0.2s, box-shadow 0.25s;
    box-shadow: 0 6px 24px rgba(246, 127, 35, 0.45);
  }

  .btn-hero-primary:hover {
    background: #d96a10;
    transform: translateY(-2px);
    box-shadow: 0 10px 30px rgba(246, 127, 35, 0.55);
  }

  .btn-hero-primary svg { transition: transform 0.25s; }
  .btn-hero-primary:hover svg { transform: translateX(4px); }

  .btn-hero-secondary {
    color: white;
    border: 2px solid rgba(255,255,255,0.6);
    background: rgba(255,255,255,0.08);
    padding: 14px 26px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    font-size: 15px;
    display: flex;
    align-items: center;
    gap: 10px;
    transition: background 0.25s, border-color 0.25s, transform 0.2s;
    backdrop-filter: blur(4px);
  }

  .btn-hero-secondary:hover {
    background: rgba(255,255,255,0.18);
    border-color: white;
    transform: translateY(-2px);
  }

  /* STATS */
  .hero-stats {
    display: flex;
    align-items: center;
    gap: 32px;
    animation: fadeDown 0.7s ease 0.52s both;
  }

  .stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 4px;
  }

  .stat strong {
    font-size: 28px;
    font-weight: 800;
    color: white;
    line-height: 1;
  }

  .stat span {
    font-size: 12px;
    color: rgba(255,255,255,0.6);
    font-weight: 500;
    letter-spacing: 0.3px;
  }

  .stat-divider {
    width: 1px;
    height: 36px;
    background: rgba(255,255,255,0.2);
  }

  /* SCROLL INDICATOR */
  .scroll-indicator {
    position: absolute;
    bottom: 32px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 6px;
    opacity: 0.5;
    animation: fadeDown 1s ease 1s both;
  }

  .scroll-line {
    width: 1.5px;
    height: 48px;
    background: linear-gradient(to bottom, white, transparent);
    border-radius: 2px;
    animation: scrollPulse 2s ease infinite;
  }

  /* ANIMACIONES */
  @keyframes fadeDown {
    from { opacity: 0; transform: translateY(-18px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  @keyframes scrollPulse {
    0%, 100% { opacity: 0.4; transform: scaleY(1); }
    50%       { opacity: 1;   transform: scaleY(1.15); }
  }

  /* RESPONSIVE MÓVIL */
  @media (max-width: 768px) {
    .hero {
      background-attachment: scroll; /* parallax off en móvil */
      padding: 110px 24px 180px;
      min-height: 100svh;
    }

    .hero-title {
      letter-spacing: -0.5px;
    }

    .br-desktop { display: none; }

    .hero-buttons {
      flex-direction: column;
      width: 100%;
      gap: 12px;
    }

    .btn-hero-primary,
    .btn-hero-secondary {
      width: 100%;
      max-width: 320px;
      justify-content: center;
      padding: 13px 18px;
      font-size: 14px;
    }

    .hero-stats {
      gap: 20px;
    }

    .stat strong { font-size: 22px; }
    .stat span   { font-size: 11px; }
    .stat-divider { height: 28px; }

    .scroll-indicator { display: none; }
  }

  @media (max-width: 400px) {
    .hero-stats {
      gap: 12px;
    }
    .stat-divider { display: none; }
    .hero-stats {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 10px;
    }
  }
</style>