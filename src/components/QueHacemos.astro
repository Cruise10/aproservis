<section class="hacemos">

  <div class="container-hacemos">

    <!-- ENCABEZADO -->
    <div class="hacemos-header">
      <span class="subtitulo">
        <svg width="12" height="12" viewBox="0 0 24 24" fill="var(--color-naranja)"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
        NUESTROS SERVICIOS
      </span>
      <h2 class="titulo-hacemos">¿Qué Hacemos?</h2>
      <p class="descripcion-hacemos">
        Ofrecemos servicios especializados en diseño, construcción y desarrollo
        de proyectos, garantizando calidad, eficiencia y resultados duraderos.
      </p>
    </div>

    <!-- GRID DE SERVICIOS -->
    <div class="grid-hacemos">

      <div class="servicio-card">
        <div class="servicio-img">
          <img src="/images/que_hacemos_2.svg" alt="Diseño Arquitectónico" loading="lazy">
        </div>
        <div class="servicio-info">
          <div class="servicio-icon">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>
          </div>
          <h3>Diseño Arquitectónico</h3>
          <p>Proyectos modernos y funcionales adaptados a cada necesidad.</p>
        </div>
      </div>

      <div class="servicio-card">
        <div class="servicio-img">
          <img src="/images/que_hacemos_1.png" alt="Construcción" loading="lazy">
        </div>
        <div class="servicio-info">
          <div class="servicio-icon">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="7" width="20" height="14" rx="2"/><path d="M16 7V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v2"/><line x1="12" y1="12" x2="12" y2="16"/><line x1="10" y1="14" x2="14" y2="14"/></svg>
          </div>
          <h3>Construcción</h3>
          <p>Ejecutamos obras con materiales de alta calidad y precisión.</p>
        </div>
      </div>

      <div class="servicio-card">
        <div class="servicio-img">
          <img src="/images/que_hacemos_1.png" alt="Calidad Garantizada" loading="lazy">
        </div>
        <div class="servicio-info">
          <div class="servicio-icon">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
          </div>
          <h3>Calidad Garantizada</h3>
          <p>Aplicamos procesos que aseguran resultados duraderos y eficientes.</p>
        </div>
      </div>

      <div class="servicio-card">
        <div class="servicio-img">
          <img src="/images/que_hacemos_3.png" alt="Seguridad Industrial" loading="lazy">
        </div>
        <div class="servicio-info">
          <div class="servicio-icon">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
          </div>
          <h3>Seguridad Industrial</h3>
          <p>Priorizamos la seguridad en cada etapa del proyecto.</p>
        </div>
      </div>

      <div class="servicio-card">
        <div class="servicio-img">
          <img src="/images/que_hacemos_1.png" alt="Remodelaciones" loading="lazy">
        </div>
        <div class="servicio-info">
          <div class="servicio-icon">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/></svg>
          </div>
          <h3>Remodelaciones</h3>
          <p>Transformamos espacios para mejorar su funcionalidad y estética.</p>
        </div>
      </div>

      <div class="servicio-card">
        <div class="servicio-img">
          <img src="/images/que_hacemos_3.png" alt="Obras Civiles" loading="lazy">
        </div>
        <div class="servicio-info">
          <div class="servicio-icon">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="1" y="3" width="15" height="13" rx="1"/><path d="M16 8h4l3 5v3h-7V8z"/><circle cx="5.5" cy="18.5" r="2.5"/><circle cx="18.5" cy="18.5" r="2.5"/></svg>
          </div>
          <h3>Obras Civiles</h3>
          <p>Infraestructura segura y eficiente para proyectos de gran escala.</p>
        </div>
      </div>

    </div>

    <!-- BOTÓN -->
    <div class="btn-hacemos">
      <a href="/servicios" class="btn-servicios">
        Ver todos los servicios
        <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
      </a>
    </div>

  </div>
</section>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Outfit:wght@400;500;600;700;800&display=swap');

  .hacemos {
    padding: 100px 20px;
    background: white;
    font-family: 'Outfit', sans-serif;
  }

  .container-hacemos {
    max-width: 1200px;
    margin: auto;
  }

  /* ENCABEZADO */
  .hacemos-header {
    text-align: center;
    margin-bottom: 64px;
  }

  .subtitulo {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    color: var(--color-naranja);
    font-weight: 700;
    letter-spacing: 2px;
    font-size: 12px;
    text-transform: uppercase;
    margin-bottom: 14px;
  }

  .titulo-hacemos {
    font-size: clamp(28px, 4vw, 42px);
    font-weight: 800;
    color: #1a1a1a;
    letter-spacing: -0.5px;
    margin: 0 0 16px;
  }

  .descripcion-hacemos {
    max-width: 580px;
    margin: 0 auto;
    color: #777;
    font-size: 16px;
    line-height: 1.7;
  }

  /* GRID */
  .grid-hacemos {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 28px;
  }

  /* CARD */
  .servicio-card {
    background: #fafafa;
    border: 1px solid #f0f0f0;
    border-radius: 14px;
    overflow: hidden;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    display: flex;
    flex-direction: column;
  }

  .servicio-card:hover {
    transform: translateY(-6px);
    box-shadow: 0 16px 40px rgba(0,0,0,0.1);
  }

  /* IMAGEN */
  .servicio-img {
    width: 100%;
    height: 180px;
    overflow: hidden;
    background: rgba(246, 127, 35, 0.05);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .servicio-img img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.4s ease;
  }

  .servicio-card:hover .servicio-img img {
    transform: scale(1.05);
  }

  /* INFO */
  .servicio-info {
    padding: 24px;
    display: flex;
    flex-direction: column;
    gap: 8px;
    flex: 1;
  }

  /* ICONO */
  .servicio-icon {
    width: 40px;
    height: 40px;
    background: rgba(246, 127, 35, 0.1);
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-naranja);
    margin-bottom: 4px;
  }

  .servicio-info h3 {
    font-size: 17px;
    font-weight: 700;
    color: #1a1a1a;
    margin: 0;
  }

  .servicio-info p {
    font-size: 14px;
    color: #777;
    line-height: 1.6;
    margin: 0;
  }

  /* BOTÓN */
  .btn-hacemos {
    margin-top: 56px;
    display: flex;
    justify-content: center;
  }

  .btn-servicios {
    display: inline-flex;
    align-items: center;
    gap: 9px;
    background: var(--color-naranja);
    color: white;
    padding: 14px 30px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    font-size: 15px;
    transition: background 0.25s, transform 0.2s, box-shadow 0.25s;
    box-shadow: 0 4px 16px rgba(246, 127, 35, 0.35);
  }

  .btn-servicios:hover {
    background: #d96a10;
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(246, 127, 35, 0.45);
  }

  .btn-servicios svg { transition: transform 0.25s; }
  .btn-servicios:hover svg { transform: translateX(4px); }

  /* RESPONSIVE */
  @media (max-width: 900px) {
    .grid-hacemos {
      grid-template-columns: repeat(2, 1fr);
      gap: 20px;
    }
  }

  @media (max-width: 560px) {
    .hacemos {
      padding: 64px 16px;
    }

    .hacemos-header {
      margin-bottom: 40px;
    }

    .grid-hacemos {
      grid-template-columns: 1fr;
      gap: 16px;
    }

    /* En móvil las cards van horizontal */
    .servicio-card {
      flex-direction: row;
      align-items: center;
    }

    .servicio-img {
      width: 100px;
      min-width: 100px;
      height: 100px;
      border-radius: 0;
    }

    .servicio-info {
      padding: 16px;
      gap: 4px;
    }

    .servicio-icon { display: none; }

    .servicio-info h3 { font-size: 15px; }
    .servicio-info p  { font-size: 13px; }
  }
</style>